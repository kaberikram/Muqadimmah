{
  "name": "gamepad-spotlight",
  "version": "2.0.0",
  "description": "Static projection-mapping spotlight driven by a gamepad — left stick moves the spot, face buttons trigger effects",
  "scripts": {
    "start": "npx --yes serve public",
    "test": "playwright test"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
